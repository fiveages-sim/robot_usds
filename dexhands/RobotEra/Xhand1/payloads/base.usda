#usda 1.0
(
    defaultPrim = "Xhand1"
    metersPerUnit = 1
    upAxis = "Z"
    doc = "Default (left) hand morphology — thin redirect to Hand/left."
)

def Xform "Xhand1" (
    prepend references = @./Hand/left/base.usda@
)
{
}
